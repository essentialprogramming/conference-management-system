export class Qualifier {
  qualifier: string;
  recommendation: string;

  constructor(qualifier: string, recommendation: string) {
    this.qualifier = qualifier;
    this.recommendation = recommendation;
  }
}
